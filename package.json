{
  "name": "how-ai-works",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The course Using AI well, built from its markdown into a WCAG AAA website on a Claude Design canvas.",
  "scripts": {
    "build": "node src/build.mjs && node scripts/static.mjs",
    "dev": "npm run --silent build && node scripts/serve.mjs",
    "look": "node scripts/look.mjs",
    "contrast": "node scripts/contrast.mjs",
    "audit": "node scripts/audit.mjs",
    "audit:quick": "node scripts/audit.mjs --quick",
    "perf": "node scripts/perf.mjs",
    "links": "node scripts/links.mjs",
    "format": "prettier --write .",
    "lint": "eslint .",
    "check:quick": "prettier --check . --log-level warn && eslint . && node scripts/contrast.mjs && npm run --silent build",
    "check": "npm run --silent check:quick && node scripts/look.mjs && node scripts/audit.mjs",
    "prepare": "git rev-parse --git-dir > /dev/null 2>&1 && git config core.hooksPath .githooks || true"
  },
  "devDependencies": {
    "@carbon/icons": "11.88.0",
    "@eslint/js": "^9.39.5",
    "axe-core": "4.13.0",
    "eslint": "^9.39.5",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.12.0",
    "marked": "14.1.4",
    "playwright": "1.63.0",
    "prettier": "^3.9.6"
  }
}
